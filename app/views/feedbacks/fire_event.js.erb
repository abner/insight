var selector = '#<%= "#{id_for_panel_feedback_detail(@feedback)} .detail_column" %>';
$(selector).fadeOut(200 , function(){
  $(selector).html("<%= escape_javascript (render partial: 'detail_column', :locals => {:feedback => @feedback}) %>");
  $(selector).fadeIn(350);
});
