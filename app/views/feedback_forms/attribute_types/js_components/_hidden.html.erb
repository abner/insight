<%= raw(
  {
      type: "hidden",
      name: attribute.name,
      value: attribute.custom_data[:default_value],
      review: false
  }.to_json + (attribute.eql?(attribute.feedback_form.feedback_attributes.last) ? '' : ',') 
)
%>
